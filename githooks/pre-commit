#!/bin/sh
npx lint-staged
if [ $? -ne 0 ]
then
  exit $?
fi

npm run lint
if [ $? -ne 0 ]
then
  exit $?
fi

npm run build
if [ $? -ne 0 ]
then
  exit $?
fi

git add dist/
if [ $? -ne 0 ]
then
  exit $?
fi
